{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/symbolicate", "destination": "/" },
    { "source": "/api/(.*)", "destination": "/" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
